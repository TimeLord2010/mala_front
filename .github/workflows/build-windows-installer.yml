name: Build Windows Installer

on:
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout mala_front
        uses: actions/checkout@v4
        with:
          path: mala_front

      # mala_api is referenced from mala_front's pubspec as ../mala_api.
      # Both repos go inside the workspace so the relative path resolves.
      - name: Checkout mala_api (path dependency ../mala_api)
        uses: actions/checkout@v4
        with:
          repository: TimeLord2010/mala_api
          path: mala_api

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: stable
          cache: true

      - name: Install Inno Setup
        shell: pwsh
        run: |
          choco install innosetup --yes --no-progress
          "C:\Program Files (x86)\Inno Setup 6" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Run build_installer.sh
        shell: bash
        working-directory: mala_front
        run: bash bash/build_installer.sh

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mala-Setup
          path: mala_front/bash/Mala-Setup-*.exe
          if-no-files-found: error
