name: mala_front
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  awesome_flutter_extensions: ^1.1.1
  brasil_fields: ^1.14.0
  file_picker: ^5.3.4
  fluent_ui: ^4.7.3
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.0
  search_cep: ^4.0.2
  vit:
    git:
      url: git@gitlab.com:vit16/vit_dart_extensions.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
